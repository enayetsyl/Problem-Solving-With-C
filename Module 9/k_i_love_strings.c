#include<stdio.h>
#include<string.h>
int main (){
  int n;
  scanf("%d", &n);

  for (int i=0; i<n; i++){
    char s[51];
    char t[51];
    char new[101];
    scanf("%s %s",s, t);
    int sl = strlen(s);
    int st = strlen(t);
    int max = 0;
    int i=0;
    if(sl>st){
      while (sl != '\0')
      {
        if(t != '\0'){
          
        }
      }
      
    }
  }


  return 0;
}